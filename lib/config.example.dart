// Copy to config.dart and fill in — config.dart is gitignored.
const apiBase = 'https://canary.staging-api.fill-easy.com';
const clientId = 'FILL_ME';
const clientSecret = 'FILL_ME';

// Where Fill Easy's callback sends the user when auth completes.
// The webview watches for this URL to know the flow is done.
const redirectUrl = 'https://www.google.com';

// 'uaepassstg' against UAE Pass staging, 'uaepass' against production.
const uaePassScheme = 'uaepassstg';
